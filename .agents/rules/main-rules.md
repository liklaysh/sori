---
trigger: always_on
---

- You can use npm, npx, tsc, zsh whenever you want; they are available at these links:
npx: /Users/liklaysh/.nvm/versions/node/v25.9.0/bin/npx;
npm: /Users/liklaysh/.nvm/versions/node/v25.9.0/lib/node_modules/npm;
tsc: /Users/liklaysh/.nvm/versions/node/v25.9.0/bin/tsc;
zsh: /bin/zsh;

- Docker is here: /usr/local/bin/docker.

- The visual style should be inherited from our system, without any transparency or glasmorphism.

- The user has granted full permission to install any necessary npm packages and dependencies as needed to fulfill technical requirements without asking for separate confirmation each time.

- The 'Sori' project is located at /Users/liklaysh/Documents/dev/voice/sori. It is a Discord-like communication app with a Neon Nocturne aesthetic, using React (Vite), Hono (Backend), Socket.io, and LiveKit.

- The user is on macOS for development.

- Break tasks into smaller subtasks and complete them one by one to avoid exceeding output tokens.

- The system must be in English; localization into other languages ​​is not required.

- Please reply in Russian in the chat.

- Stick to the style of our system as we defined in CSS.

- After completing the task, be sure to check the Problems tab in IDE to fix them.